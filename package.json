{
  "name": "truxt-outbound-engine",
  "version": "0.1.0",
  "description": "Truxt outbound discovery-to-outreach engine with human review gate",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "cli": "node --env-file-if-exists=.env cli.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0",
    "express": "^4.21.0"
  }
}
